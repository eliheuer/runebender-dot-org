# Runebender Documentation

A plain-text bundle of the Runebender documentation at
https://runebender.org/docs, generated from the same pages.

Runebender is a free, open-source font editor for UFO and designspace
sources, built on the Linebender ecosystem of Rust crates. A headless
core does the font work, and a GPUI front-end runs both in the browser
as WebAssembly and as a native application from the same code.

It is alpha software. Treat this as an orientation map, not a stable
API contract, and verify behaviour against the source.

Source: https://github.com/eliheuer/runebender-gpui
Shared core: https://github.com/eliheuer/runebender-core
License: Apache-2.0 or MIT


## Install
https://runebender.org/docs/install.html

Runebender runs in a browser with nothing to install. To open fonts from your own disk, build the native application with cargo.

### In the Browser

Open [runebender.org/gpui](/gpui/index.html). The editor loads as
WebAssembly and runs on WebGPU. Nothing is installed and nothing is
uploaded.

The browser build is experimental. Some parts do not work there yet:
text fields outside the glyph search box cannot take focus, in-window
menu items do not activate, and paste is unavailable. Use the native
application for real work.

### Native

You need a Rust toolchain. Install one from
[rustup.rs](https://rustup.rs) if you do not have it.

Install the editor.

    cargo install --locked --git https://github.com/eliheuer/runebender-gpui

Then open a font.

    runebender-gpui path/to/Font.designspace

Runebender opens .designspace files, .ufo directories, and a
directory that holds them. Start it without a path to get an empty
window, then use File, Open.

Use --locked. Several dependencies track a git branch, and the
committed lockfile holds the revisions the editor is known to build
against. Without it, cargo resolves those dependencies to whatever
the branch tip is today.

### Toolchain Notes

The repository pins a stable Rust toolchain in rust-toolchain.toml,
and cargo respects that pin automatically. Do not force a nightly
toolchain: a GPUI dependency, pathfinder_simd, does not compile on
current nightlies.

Every dependency comes from crates.io or a public git repository, so a
plain clone builds with no extra setup.

### A New Font

Press Cmd+N to start a new font. Runebender creates an
untitled UFO with Google Fonts metrics and the Latin Core glyph set as
empty encoded glyphs, then asks where to save it.


## First Steps
https://runebender.org/docs/first-steps.html

Open a font, find a glyph, draw something, and save it. This page is the shortest path from a fresh install to a changed file on disk.

### Open a Font

Work on a copy. Runebender writes to your source files in place.

1. Start the editor with a path, or use File, Open.
2. Point it at a .designspace file, a .ufo directory, or a folder
that holds them.
3. The glyph grid fills the window.

A designspace opens every master it names. A single UFO opens as one
master.

### Find a Glyph

The left panel lists categories, languages, and saved filters. Type in
the search box to narrow the grid by name or by Unicode value. The
buttons beside the box switch the search between name, Unicode, and
everything, and turn on regular expressions or case matching.

Double-click a cell to open that glyph.

### Draw

Press the pen tool in the toolbar. Click to place corner points. Click
and drag to place a point and pull its outgoing handle. Click the
first point again to close the contour.

Switch to the select tool to move points. Drag a point to move it,
drag a handle to reshape the curve, and drag a marquee to select
several points at once.

Press Cmd+Z to undo. Every edit session keeps its own
undo stack.

### See It in Text

Press the text tool. Type, and the glyphs of the current font lay out
in a line with real kerning. Click a glyph in that line to edit it in
place, with its neighbours still visible around it.

### Save

Press Cmd+S. Runebender writes the open UFO or
designspace back to disk and shows the time in the header.

### Next

- The Interface: What every panel and bar does.
- Tools: The other six edit modes.
- Masters: Work across a designspace.


## The Interface
https://runebender.org/docs/interface.html

Runebender has two views: a grid of every glyph in the font, and an editor for one glyph in context. The panels around them stay the same in both.

### Two Views

The **grid** shows every glyph in the font as a cell. It is where you
browse, search, filter, select several glyphs at once, and apply marks.

The **editor** shows one glyph with its points, handles, and metrics.
Its neighbours are drawn around it, so you always see the glyph in
context rather than alone on a page.

Double-click a cell to enter the editor. The tab strip above the
canvas holds several edit sessions at once. Press the plus to open
another session on the current glyph. Each session keeps its own
buffer, tool, selection, viewport, and undo stack, and follows a glyph
by name across renames and master switches.

### Left Panel

Left panel:
- Search: Filter by name or Unicode value. The buttons beside the box set the scope and turn on regular expressions and case matching.
- Categories: Unicode categories: letters, marks, numbers, punctuation, symbols.
- Languages: Language and script rows with Google Fonts coverage. A row missing glyphs shows a plus that adds them, named and encoded, to every master.
- Filters: Saved filters. A filter you build from the search controls can be kept and reused; they are stored in the font.
- Sort: Order the grid by name or by Unicode value.

### Right Panel

The right panel changes with what is selected and which tool is
active. Sections appear and disappear rather than greying out.

Right panel sections:
- Preview: The current glyph drawn large, with an anatomy view: outline, handle lines, points, and a metric frame.
- Glyph: Name, Unicode value, and left and right kerning groups. Editing the name updates components in other glyphs, group membership, kerning pairs, and the open text session.
- Selection: Coordinates for what is selected, a nine-point reference picker, and numeric move and scale fields. Works on points, a component, or an anchor.
- Masters: One row per master, each showing the current glyph as that master draws it. Axis sliders sit above.
- Background: Show, send, swap, or clear the background layer, and name a reference glyph to ghost behind the drawing.
- Measure: Appears with the measure tool. Toggles for outline colouring, handle and segment lengths, stems and counters, side bearings, and sums.
- Curves: Curvature comb and continuity display.
- Transformations: Flip, rotate, and duplicate tiles.

### Bottom Bar

The bottom bar carries the metrics of the current glyph: advance
width, left and right side bearings, and the current master. Editing a
field moves the outline or the sidebearing directly.

In the grid, the bottom bar shows the current selection and offers to
copy it as text.

### Header

The header shows the path of the open font and the state of the file.
After a save it reads the time, so you can tell at a glance whether
what is on screen is on disk.


## Tools
https://runebender.org/docs/tools.html

Eight edit modes. Select and pen cover most work; the rest are for cutting, measuring, previewing, and typing.

### Select

Drag a point to move it. Drag a handle to reshape the curve. Drag on
empty canvas for a marquee. Shift extends the selection.

Arrow keys nudge the selection. Hold a modifier for the larger steps.

Drag the sidebearing edges to change spacing without touching the
outline. Drag a component to move it, and double-click it to open the
glyph it points at.

Cmd+A selects every point, Cmd+Alt+A deselects.

### Pen

Click to place a corner point. Click and drag to place a point and
pull its outgoing handle out of it. Click the first point of the
contour to close it.

### Hyperbezier Pen

Draws hyperbezier contours: curves defined by the points you place
rather than by handles you position. Use Path, Hyperbezier to Cubic to
convert a hyperbezier contour into ordinary cubic segments. With
nothing selected, that command converts every hyperbezier contour in
the glyph.

### Shapes

Drag a rectangle or an ellipse. Hold shift to constrain it to a square
or a circle.

### Knife

Drag a line across the outline to cut it. Hold shift to lock the cut
to an axis.

### Measure

Drag a line to measure across the glyph. Hold shift to lock it to an
axis. The Measure section in the right panel turns on outline
colouring, handle lengths, segment lengths, stems and counters, side
bearings, and running sums.

### Text

Type, and the font lays out as real text with kerning applied. Click a
glyph in the line to edit it in place. Editing changes propagate to
the line as you work.

Text handles bidirectional scripts. Arrow keys move the caret
visually, not by storage order, so left means left on screen in Arabic
and Hebrew. See [Text and Shaping](/docs/text.html).

### Preview

Hides points, handles, and metric guides so you see the shapes alone.

### Shift

Shift constrains the shapes, knife, and measure tools. It does not
constrain point drags: there it extends the selection.


## Glyphs
https://runebender.org/docs/glyphs.html

Add, rename, encode, group, and remove glyphs. Renaming is safe: Runebender follows the name everywhere it is used.

### Add and Remove

Glyph menu:
- New Glyph: Cmd+Alt+Shift+N. Adds an empty glyph to every master.
- Duplicate Glyph: Copies the current glyph under a new name.
- Remove Glyph: Deletes it from every master.

A language row in the left panel that is missing glyphs shows a plus.
Pressing it adds every missing glyph for that language, named and
encoded, to every master at once.

### Name, Unicode, Groups

The Glyph section of the right panel holds the name, the Unicode
value, and the left and right kerning groups.

Unicode values are accepted as 0041, U+0041, or 0x41.

Renaming updates every place the old name was used: components in
other glyphs, kerning group membership, kerning pairs, the open text
session, and the current selection. Kerning group edits are written to
groups.plist for every master, and clearing a field removes the
membership.

Renaming and Unicode editing need the native application. Text
fields cannot take focus in the browser build yet.

### Marks and Selection

Cells in the grid take colour marks. Cmd-click toggles a cell,
shift-click selects a range in the order shown, and a mark applies to
the whole selection. The footer copies the selection as text.

### Metrics

Advance width and side bearings live in the bottom bar. Editing a
field moves the outline or the sidebearing. Glyph, Update Metrics
recalculates them, and Cmd+Ctrl+M does the same.

### Images

Image commands:
- Place Image…: Puts a bitmap behind the glyph as a reference.
- Trace Image…: Converts the placed bitmap into contours and replaces what the glyph holds. Undoable.
- Remove Image: Takes the bitmap away.
- Import SVG…: Brings an SVG outline in as contours.
- Export Glyph as SVG: Writes the current glyph out as an SVG file.

The Background section of the right panel is separate from a placed
image. It shows, sends to, swaps with, or clears the UFO
public.background layer, which is saved with the font. It can also
ghost another glyph behind the drawing, named by hand.


## Paths and Filters
https://runebender.org/docs/paths.html

Clean up outlines, combine them, transform them, and convert between curve types. Everything here works on the selection, or on the whole glyph when nothing is selected.

### Clean Up

Path menu, cleanup:
- Tidy Up Paths: Cmd+Shift+T. Drops zero-length segments.
- Add Extremes: Inserts points at the extremes of each curve.
- Round Coordinates: Snaps every coordinate to whole units.
- Correct Path Direction: Cmd+Shift+R. Sets outer contours counter-clockwise and holes clockwise, by containment depth.
- Reverse Contours: Cmd+Alt+Shift+R. Flips direction by hand.
- Set Start Point: Makes the selected point the first point of its contour.

### Combine

Boolean operations:
- Remove Overlap: Cmd+Shift+O. Merges overlapping contours into one outline.
- Union: Adds the selected contours together.
- Subtract: Cuts the later contours out of the first.
- Intersect: Keeps only the area they share.
- Exclude: Keeps only the area they do not share.

### Transform

Transformations:
- Flip Horizontal: Cmd+Shift+H
- Flip Vertical: Cmd+Shift+V
- Rotate: 90 degrees left, 90 degrees right, or 180 degrees.
- Duplicate Selection: Cmd+D. Copies contours, components, and anchors.
- Duplicate + Repeat: Cmd+Alt+Shift+T. Duplicates and re-applies the last flip or rotation.

For an exact move or scale, use the Selection section of the right
panel. Pick a reference point on the nine-point grid, then set X and Y
to move the selection so the reference lands there, or W and H to
scale about it.

### Curve Quality

Curve commands:
- Harmonize: Makes the curvature continuous across an on-curve point.
- Balance: Evens out the handles of a segment.
- Optimize: Reduces the point count where the shape allows it.

The Curves section of the right panel draws a curvature comb and a
continuity display, so you can see the result rather than guess at it.

### Convert

Curve type:
- Hyperbezier to Cubic: Converts hyperbezier contours to ordinary cubic segments.
- Quadratic to Cubic: For sources that came from TrueType outlines.
- Cubic to Quadratic: For output that needs them.

### Filters

Filters reshape an outline rather than clean it up.

Filter menu:
- Offset Curve: Grows or shrinks the outline by a distance.
- Extrude: Pushes the shape out along a direction.
- Roughen: Breaks smooth edges into a rougher line.
- Round Corners: Fillets the selected corners. The size and handle ratio follow the corners already in the glyph.
- Slanter: Slants the outline, for oblique work.

### Context Menu

Right-click on the canvas for the operations that apply to what is
under the pointer: lock and unlock a component, decompose one or all,
add a component, set the start point, reverse the contour, round
corners, move a contour up or down in the order, and add or delete an
anchor.


## Components and Anchors
https://runebender.org/docs/components.html

Build accented and composite glyphs out of other glyphs. Anchors hold the pieces together, and marks follow their base as you edit it.

### Components

A component places another glyph inside this one. Edit the base glyph
and every glyph using it follows.

Add a component from the canvas context menu and type the base glyph
name. Select and drag a component with the select tool, and
double-click it to open the glyph it points at. Delete removes it.

Component commands:
- Decompose Components: Cmd+Shift+D. Replaces components with their outlines.
- Decompose One: Context menu. Only the component under the pointer.
- Lock / Unlock: Context menu, or the Lock and Free toggle in the Selection panel.

### Anchors and Auto-Alignment

An anchor is a named point that says where a mark attaches. Give the
base glyph an anchor, give the mark a matching one, and the mark
places itself.

Composites align live. When you move the anchor on a base glyph, every
accented glyph built from it follows immediately. A component that is
locked to an anchor refuses to be dragged, because its position comes
from the anchor rather than from you. Use the Lock and Free toggle in
the Selection panel when you need to override that.

A component added from the context menu arrives anchor-locked, so
marks snap to their anchor without extra work.

### Editing Anchors

Add and delete anchors from the canvas context menu. Select one with
the select tool and drag it, or set exact coordinates in the Selection
panel: with an anchor selected, the X and Y fields address the anchor
itself. Its name is a field in the same panel.

### Joining Scripts

Glyph, Check Joining reports whether the joining forms of a
cursive script are present and correctly named. Use it on Arabic and
other connected scripts before assuming a shaping problem is a
shaping problem. See [Text and Shaping](/docs/text.html).


## Masters and Interpolation
https://runebender.org/docs/masters.html

A designspace opens every master it names. Axis sliders move you through the space, and the editor draws what falls between.

### Masters

Open a .designspace file and Runebender opens every master it names.
Open a single .ufo and you get one master.

The Masters section of the right panel lists them. Each row draws the
current glyph as that master draws it, so you can compare without
switching. Click a row to switch to it. View, Next Master and View,
Previous Master step through them from the keyboard.

View, Show All Masters draws every master at once over the current
glyph.

### Axes

Axis sliders sit above the master list. Moving one shows the
interpolated result as a ghost over the drawing, so you can see what a
weight between two masters looks like while you edit either one.

### Reinterpolate

Glyph, Reinterpolate replaces the current glyph with what the
surrounding masters say it should be at this position. It is the way
back when a glyph has drifted out of step with the rest of the
designspace.

Reinterpolate overwrites the glyph in the current master. Undo
brings it back.

### Compatibility

Interpolation needs matching outlines: the same number of contours,
the same number of points in each, in the same order, starting at the
same place. When a glyph does not interpolate, that is almost always
why.

Path, Correct Path Direction and Path, Set Start Point fix the two
most common causes. Tidy Up Paths removes zero-length segments, which
can differ between masters without being visible.

### Smart Components

A smart component carries its own axes, so one component can hold a
range of shapes rather than a single fixed one. Runebender reads and
writes the glyphsLib keys for them, so sources that came out of Glyphs
keep working.

Interpolation across several axes uses a corner-delta model: the
default shape plus the contribution of each axis corner. One axis
interpolates linearly, and two axes with every corner present
interpolate bilinearly.

### Masks

Glyph, Bake Masks resolves the intermediate layers of a glyph into
ordinary outlines. Use it when a source needs to be read by a tool
that does not understand them.


## Text and Shaping
https://runebender.org/docs/text.html

Type with the font you are drawing. The text tool shapes real text, applies kerning, and handles bidirectional and connected scripts, so Arabic work is not an afterthought.

### Typing

Press the text tool and type. The glyphs of the open font lay out as a
line of real text, shaped and kerned. Click a glyph in that line to
edit it in place, with its neighbours around it.

Edits show up in the line as you make them. The glyph you are editing
uses its own live advance width, so spacing changes are visible in
context immediately.

View, Next Sample String cycles through sample strings.

### Bidirectional Text

The caret moves visually. Left means left on the screen and right
means right, whichever direction the run is written in. Home and End
go to the visual edges of the line.

Arabic re-shapes around the caret as you type and as you delete, so
joining forms stay correct while the text changes.

### Connected Scripts

Arabic and other cursive scripts need the joining forms of each
letter: initial, medial, final, and isolated. Glyph, Check Joining
reports which forms a glyph is missing and whether the names follow
the expected convention.

When a word does not join correctly, check the glyph names first.
Shaping engines find joining forms by name, so a misnamed glyph
looks to them like a missing one.

### Kerning

Kerning is edited by dragging. Put two glyphs next to each other in
the text line and drag the space between them.

Kerning groups let one value cover many pairs. Set the left and right
group of a glyph in the Glyph section of the right panel. Groups are
written to groups.plist for every master.

### Script and Language

Shaping can be run under a chosen script and language, rather than
letting the engine guess from the characters. That matters where a
language changes the expected forms, and it is how you check that a
feature fires for the language it was written for.


## Shortcuts
https://runebender.org/docs/shortcuts.html

Every keyboard command in the editor, grouped by the menu it belongs to. On Windows and Linux, read Cmd as Ctrl.

### File

File:
- Cmd+N: New font
- Cmd+O: Open
- Cmd+S: Save
- Cmd+Shift+S: Save as
- Cmd+E: Export
- Cmd+Q: Quit

### Edit

Edit:
- Cmd+Z: Undo
- Cmd+Shift+Z: Redo
- Cmd+C: Copy
- Cmd+V: Paste
- Cmd+A: Select all points
- Cmd+Alt+A: Deselect all points

### Glyph

Glyph:
- Cmd+Alt+Shift+N: New glyph
- Cmd+Shift+D: Decompose components
- Cmd+Ctrl+M: Update metrics

### Path

Path:
- Cmd+Shift+T: Tidy up paths
- Cmd+Shift+O: Remove overlap
- Cmd+Shift+R: Correct path direction
- Cmd+Alt+Shift+R: Reverse contours
- Cmd+Shift+H: Flip horizontal
- Cmd+Shift+V: Flip vertical
- Cmd+D: Duplicate selection
- Cmd+Alt+Shift+T: Duplicate and repeat

### View

View:
- Cmd+0: Zoom to fit

The rest of the View menu has no key: show all masters, sort glyphs by
name, sort glyphs by Unicode, next master, previous master, and next
sample string.

### Text Mode

Caret:
- Left, Right: Move the caret visually, not by storage order.
- Up, Down: Move a line.
- Home, End: Go to the visual edges of the line.
- Backspace, Delete: Delete, re-shaping Arabic around the caret.
- Enter: Line break.

### Menus Without Shortcuts

Everything in the menus works whether or not it has a key. The Path
and Filter menus hold many operations that do not: booleans,
extremes, rounding, harmonize, balance, optimize, curve conversion,
offset, extrude, roughen, round corners, and slant.

In the browser build, in-window menu items do not activate yet. Use
the keyboard commands there.


## File Format
https://runebender.org/docs/format.html

Runebender edits UFO and designspace sources in place. It writes standard data where a standard exists, and namespaced keys where one does not.

### Sources

Runebender opens .designspace files, .ufo directories, and folders
that hold them. It saves in the same format, in place.

UFO is a directory of files, so a font source works with version
control. Commit your sources and you can see, review, and undo a day's
drawing the way you would any other change.

Runebender writes to your files. Keep them in version control and
work on copies while the editor is alpha.

### Standard Data

Standard UFO data:
- Outlines: GLIF contours, points, and components.
- Anchors: Standard GLIF anchors.
- Kerning: kerning.plist and groups.plist, per master.
- public.kern: Group names follow the standard prefixes.
- public.background: The background layer, saved with the font.
- public.postscriptNames: Production names, used on export.

### Glyphs Compatibility

Sources that came out of Glyphs keep their smart component data.
Runebender reads and writes the glyphsLib keys rather than inventing
its own.

glyphsLib keys:
- com.schriftgestaltung.Glyphs.smartComponentAxes: The axes a smart component carries.
- com.schriftgestaltung.Glyphs.componentsSmartComponentValues: Where a placed component sits in those axes.
- com.schriftgestaltung.Glyphs.glyph.leftMetricsKey: Metric keys, left and right.

### Runebender Keys

Anything Runebender needs that UFO does not define lives under its own
namespace. Another editor ignores these keys, and the rest of the
source still reads correctly.

com.runebender keys:
- savedFilters: Filters saved in the left panel.
- annotations: Notes left on a glyph.
- masks: Intermediate mask layers.
- hoiIntermediate: Higher-order interpolation intermediates.
- partSelection: Which part of a smart component is in use.

### Hyperbeziers

A hyperbezier contour is defined by its on-curve points, with the
control points solved rather than placed. Runebender marks these
contours in GLIF with identifier="hyperbezier".

An editor that does not know the identifier reads the points as
ordinary curve points, so the file stays valid. Use Path, Hyperbezier
to Cubic before handing a source to a tool that needs plain cubics.


## Exporting
https://runebender.org/docs/exporting.html

Export builds a font file from your sources. If the project has a build script, Runebender runs that. Otherwise it compiles with fontc.

### Export

Press Cmd+E, or use File, Export. Unsaved masters are
saved first, so what you export is what is on disk.

### A Build Script Wins

Runebender looks for build-fontc.sh or build.sh beside your
source, then in each parent directory, stopping at the repository
root. If it finds one, it runs that script and reports where the
output landed.

This is deliberate. A project that already builds a specific way
should keep building that way, and the editor should not be a second
opinion about how your font is made. Google Fonts projects usually
have such a script already.

The script runs with a repaired PATH, so a project .venv, cargo
binaries, and Homebrew are all reachable even when the application was
launched from the Dock.

### Otherwise fontc

With no build script, Runebender compiles the source with
[fontc](https://github.com/googlefonts/fontc) and writes the result
next to it.

Install fontc if you do not have it.

    cargo install fontc

Runebender looks for fontc on your PATH, then in
~/.cargo/bin. If neither has it, export stops and says so.

### Production Names

Names in public.postscriptNames are applied on export, so working
names in your source do not have to be the names that ship.

### Single Glyphs

Glyph, Export Glyph as SVG writes the current glyph as an SVG file
beside the source. It is for a drawing you want outside the font, not
part of the font build.

Export and SVG export are native only. The browser build cannot
write to your disk.


## Troubleshooting
https://runebender.org/docs/troubleshooting.html

What to check when the editor will not build, will not open a font, or does something you did not expect.

### It Will Not Build

If the build fails inside pathfinder_simd, you are on a nightly
toolchain. The repository pins a stable one in rust-toolchain.toml.
Remove any override and let the pin apply.

If cargo cannot resolve dependencies, or the build fails in a
dependency you did not change, you probably installed without
--locked.

    cargo install --locked --git https://github.com/eliheuer/runebender-gpui

### A Font Will Not Open

Runebender opens .designspace files, .ufo directories, and folders
that hold them. It does not open compiled .ttf or .otf files.

A designspace that names a master file which is not there will not
open. Check the filename of each source in the designspace.

### A Glyph Will Not Interpolate

Interpolation needs matching outlines across masters: the same number
of contours, the same number of points in each, in the same order,
starting at the same point.

1. Run Path, Correct Path Direction in each master.
2. Use Path, Set Start Point so each contour starts in the same place.
3. Run Path, Tidy Up Paths to remove zero-length segments, which
differ between masters without being visible.
4. Compare the point counts in the Masters section.

### A Word Does Not Join

Cursive scripts find their joining forms by glyph name. A misnamed
glyph looks to a shaping engine like a missing one.

Run Glyph, Check Joining. It reports which of the initial, medial,
final, and isolated forms are present and whether the names follow
the expected convention.

### The Browser Build

These limits are in the browser build only. The native application
is unaffected. Use it for real work.

Known browser limits:
- Text fields: Cannot take focus, except the glyph search box. Metrics fields and the Glyph panel fields do not accept typing.
- Menus: In-window menu items do not activate. Use the keyboard commands.
- Paste: Reading the clipboard is not implemented.
- Script icons: Show as empty boxes. The bundled interface font has no Arabic or Hebrew.
- Export: Not available. The browser cannot write to your disk.

The browser build also needs cross-origin isolation, because it uses
shared memory. On runebender.org that is arranged by a service worker,
since GitHub Pages cannot send the headers itself. A browser that
blocks service workers will not load the editor.

### Reporting a Problem

Open an issue on
[the repository](https://github.com/eliheuer/runebender-gpui/issues).

A useful report says what you did, what happened, and what you
expected. Name the file you had open and whether you were in the
browser or the native application. If the editor wrote something
wrong into a source, the diff from version control is the best
evidence there is.


## Development
https://runebender.org/docs/development.html

Build from source, work on the editor and the shared core together, and build the browser bundle.

### Repositories

Where the code lives:
- runebender-gpui: The editor. Runs in the browser and as a native application from one codebase.
- runebender-core: The shared library: editing model, shaping, measurement, curve analysis. No interface.
- runebender-xix: The same editor on xix, a fork of Xilem. It exists to compare the two GUI stacks on one application.
- runebender-druid: The original implementation, kept as project history.

The core crate holds everything that does not need a window, so the
same font logic serves any front-end put on top of it.

### Build and Run

    git clone https://github.com/eliheuer/runebender-gpui
    cd runebender-gpui
    cargo run

The repository pins a stable Rust toolchain in rust-toolchain.toml.
Cargo applies it on its own.

### Working on Core Too

Clone the core crate beside the editor, then add a cargo paths
override so the local copy replaces the published one.

    git clone https://github.com/eliheuer/runebender-core

toml
# .cargo/config.toml, in the directory that holds both checkouts
paths = ["runebender-core"]

Put that file above the two repositories, not inside either one. The
override is a local development setting, and committing it would
break the build on every other machine.

### The Browser Build

The browser build needs a different toolchain from the native one.

sh
RUSTUP_TOOLCHAIN=nightly-2026-08-01 \
CARGO_UNSTABLE_BUILD_STD="std,panic_abort" \
trunk build --release --public-url /gpui/

Two constraints decide that line. The web backend needs wasm atomics
and shared memory, and the prebuilt wasm32-unknown-unknown standard
library has neither, so std must be rebuilt. That is -Z build-std,
which is nightly only. The nightly must also be newer than the pinned
stable, because gpui uses library features that stable already has.

Install rust-src and the wasm target for whichever nightly you use.

To deploy, copy dist/ into this site's public/gpui/. Keep
coi-serviceworker.min.js and its script tag at the top of :
GitHub Pages cannot send the COOP and COEP headers that shared memory
requires, so the worker sets them in the browser instead.

### Dependencies

Every dependency comes from crates.io or a public git repository, so a
plain clone builds with no local paths.

GPUI and gpui_platform come from the Zed git repository rather than
crates.io, because the editor shell uses
[gpui-component](https://github.com/longbridge/gpui-component), which
tracks Zed main.

### License

Apache-2.0 or MIT.


## Glossary
https://runebender.org/docs/glossary.html

Terms used across these pages, in the sense Runebender uses them.

### Sources

Files and formats:
- UFO: Unified Font Object. A font source made of directories and files, so it works with version control.
- GLIF: The XML file that holds one glyph inside a UFO.
- Designspace: A file that names several masters and the axes between them.
- Master: One drawn source at a fixed position in the designspace.
- Axis: A dimension of variation, such as weight or width.

### Drawing

Outlines:
- Contour: One closed or open path in a glyph.
- On-curve point: A point the outline passes through.
- Handle: An off-curve control point that shapes the segment beside it.
- Cubic: A curve with two handles per segment. What UFO and PostScript use.
- Quadratic: A curve with one handle per segment. What TrueType uses.
- Hyperbezier: A curve defined by its on-curve points, with the handles solved rather than placed.
- Extreme: The topmost, bottommost, leftmost, or rightmost point of a curve.
- Path direction: Which way a contour is drawn. Outer contours and holes must run opposite ways.
- Start point: The first point of a contour. Masters must agree on it to interpolate.

### Building Glyphs

Composites:
- Component: Another glyph placed inside this one.
- Composite: A glyph built from components rather than drawn.
- Decompose: Replace components with copies of their outlines.
- Anchor: A named point that says where a mark attaches.
- Smart component: A component that carries its own axes, so one component holds a range of shapes.

### Spacing and Text

Metrics and shaping:
- Advance width: How far the pen moves after drawing the glyph.
- Sidebearing: The space between the outline and the edge of the advance, left or right.
- Kerning: An adjustment applied to one pair of glyphs.
- Kerning group: A set of glyphs that kern alike, so one value covers many pairs.
- Shaping: Turning characters into positioned glyphs, applying the font's own rules.
- Bidirectional: Text that mixes directions, such as Arabic with Latin inside it.
- Joining form: The shape a cursive letter takes by position: initial, medial, final, or isolated.

### Output

Building a font:
- Compile: Turn sources into a font file a system can install.
- fontc: The Rust font compiler Runebender uses when a project has no build script.
- Production name: The glyph name that ships, which can differ from the working name.
- Variable font: One font file that holds a range along its axes rather than a single instance.
